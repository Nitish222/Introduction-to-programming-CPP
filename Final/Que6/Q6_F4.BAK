#include<iostream.h>
#include<conio.h>
#include<string.h>

void main()
{
	clrscr();
	char str1[50] = "Hello";
	char str2[50] = "Wello";
	char *pt = strstr(str1, str2);

	if(pt)
		cout<<str2<<" is sub string of "<<str1<<"\n";
	else
		cout<<str2<<" is not substring of "<<str1<<"\n";
	getch();
}
