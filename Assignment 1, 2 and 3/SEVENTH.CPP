#include <iostream.h>
#include <conio.h>

void main()
 {
  clrscr();
  int a,b,sum=0;
  cout<<"\nEnter the first number: ";
  cin>>a;
  cout<<"Enter the second number: ";
  cin>>b;
  sum = a + b;
  cout<<"The sum of "<<a<<" and "<<b<<" is: "<<sum;
  getch();
 }